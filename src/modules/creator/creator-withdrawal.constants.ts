/** Minimum wallet balance required to submit a withdrawal request (creators and staff). */
export const MIN_WITHDRAWAL_COINS = 1000;

/** @deprecated Use MIN_WITHDRAWAL_COINS — kept for existing imports. */
export const MIN_CREATOR_WITHDRAWAL_COINS = MIN_WITHDRAWAL_COINS;

export const MIN_STAFF_WITHDRAWAL_COINS = MIN_WITHDRAWAL_COINS;
